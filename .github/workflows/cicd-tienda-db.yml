name: CI/CD Tienda Perritos - DB

on:
  push:
    branches: [ "main" ]
    paths:
      - "db/**"

jobs:
  build-push-deploy-db:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      # Login en Docker Hub
      - name: Login en Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build y Push a Docker Hub
      - name: Build imagen DB y push a Docker Hub
        run: |
          cd db
          docker build -t ${{ secrets.DOCKER_USER }}/tienda-db:latest .
          docker push ${{ secrets.DOCKER_USER }}/tienda-db:latest

      # Configurar AWS para usar SSM
      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      # Despliegue en EC2
      - name: Desplegar DB vía SSM
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_DB_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --comment "Actualizar tienda-db desde Docker Hub" \
            --parameters '{"commands":[
              "set -e",
              "sudo docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASSWORD }}",
              "sudo docker pull ${{ secrets.DOCKER_USER }}/tienda-db:latest",
              "sudo docker stop tienda-db || true",
              "sudo docker rm tienda-db || true",
              "sudo docker run -d --name tienda-db -p 3306:3306 -v dbdata:/var/lib/mysql ${{ secrets.DOCKER_USER }}/tienda-db:latest"
            ]}' \
            --output text